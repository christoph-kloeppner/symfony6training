<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Sensio TV+</a>

        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item" style="color: white;">
                CURRENT-Route: {{ current??'unknown!' }}
            </li>
            <li class="nav-item">
                <a class="nav-link{{ app.current_route == 'app_main_index' ? ' active' }}" href="/">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{{ app.current_route == 'app_movie_show' ? ' active' }}" href="{{ path('app_movie_show') }}">Movie</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{{ app.current_route == 'app_book_index' ?' active' }}" href="{{ path('app_book_index') }}">Book</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{{ app.current_route == 'app_main_contact' ? ' active' }}" href="{{ path('app_main_contact') }}">Contact</a>
            </li>
        </ul>
    </div>
</nav>